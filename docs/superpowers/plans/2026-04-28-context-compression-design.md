# LightHermes 上下文压缩系统设计

**日期**: 2026-04-28  
**版本**: v0.3.0 规划  
**状态**: 设计中（待明天继续）

---

## 背景

LightHermes 当前缺少自动上下文管理机制，长对话和大量记忆会导致：
1. Token 溢出，无法继续对话
2. 记忆检索效率下降
3. API 调用成本增加

## 设计目标

**核心原则**：保持轻量（< 300 行代码），借鉴 hermes-agent 核心思路

**主要场景**：
- **A. 长对话压缩** - CLI 中长时间对话导致的 token 溢出
- **B. 记忆系统压缩** - 压缩存储的情景记忆，减少检索开销

**触发方式**：
- **C. 混合模式** - 自动检测（token 阈值）+ 手动命令（`/compress`）

**压缩策略**：
- 待明天讨论确定

---

## 参考实现分析

### Hermes-agent 核心算法

**文件**: `hermes-agent/agent/context_compressor.py` (~1300 行)

**核心流程**：
1. **工具输出预剪枝** - 便宜，无 LLM 调用
   - 旧工具结果替换为占位符
   - 保留工具调用信息，删除详细输出
   
2. **保护头部消息** - 系统提示 + 首次交互
   - `protect_first_n = 3` 条消息
   
3. **保护尾部消息** - 按 token 预算
   - 约 20K tokens 的最近对话
   - 动态计算，不是固定消息数
   
4. **中间对话总结** - 使用辅助模型（便宜/快速）
   - 结构化摘要模板（已解决/待处理问题）
   - 摘要预算：`min(2000, compressed_content * 0.20, 12000)` tokens
   
5. **迭代摘要更新** - 多次压缩时更新而非重新总结
   - 保留之前的摘要，追加新内容
   - 避免信息丢失

**关键特性**：
- 摘要前缀标记：`[CONTEXT COMPACTION — REFERENCE ONLY]`
- 防止模型混淆摘要和当前任务
- Token 估算：`chars / 4` 粗略估计

### LightAgent 分析

**文件**: `LightAgent/LightAgent/la_core.py`

**发现**：
- 没有专门的上下文压缩实现
- 主要依赖基础的对话历史管理
- 可借鉴的内容较少

---

## 设计方向（待明天细化）

### 1. 长对话压缩（优先级：高）

**目标**：解决 CLI 中长时间对话的 token 溢出

**核心组件**：
- `ContextCompressor` 类（< 200 行）
- 简化版 hermes-agent 算法

**待讨论**：
- 压缩后内容如何处理？
  - A. 仅保留摘要（节省空间，信息有损）
  - B. 摘要 + 归档（可追溯，增加存储）
  - C. 分层存储（核心摘要 + 详细归档 + 原始备份）
  - D. 先做 A，简单直接

### 2. 记忆系统压缩（优先级：中）

**目标**：压缩情景记忆，减少检索开销

**待讨论**：
- 与长对话压缩的关系
- 压缩触发时机（记忆数量阈值？时间阈值？）
- 压缩粒度（单条记忆？批量记忆？）

### 3. 自动检测机制

**待实现**：
- Token 计数（使用 tiktoken 或简单估算）
- 阈值配置（默认 75% 上下文窗口）
- 压缩触发逻辑

### 4. 手动命令

**待实现**：
- `/compress` - 立即压缩当前对话
- `/compress <focus>` - 带焦点的压缩（可选）

---

## 技术细节（待明天细化）

### Token 估算

**选项 1**：使用 tiktoken（精确但增加依赖）
```python
import tiktoken
encoder = tiktoken.encoding_for_model("gpt-4")
tokens = len(encoder.encode(text))
```

**选项 2**：简单估算（轻量但不精确）
```python
tokens = len(text) // 4  # 粗略估计
```

**决策**：待讨论

### 摘要模型选择

**选项 1**：使用主模型（准确但贵）
**选项 2**：使用便宜模型（如 gpt-4o-mini）
**选项 3**：可配置（默认便宜模型）

**决策**：倾向选项 3

### 存储策略

**待讨论**：
- 压缩后的摘要存储在哪里？
  - 内存中（临时）
  - 文件中（持久化）
  - 记忆系统中（与现有记忆集成）

---

## 实施计划（草案）

### Phase 1: 基础压缩（1-2 天）
1. 实现 `ContextCompressor` 基础类
2. 实现简单的 token 估算
3. 实现手动 `/compress` 命令
4. 单元测试

### Phase 2: 自动检测（1 天）
1. 实现 token 计数和阈值检测
2. 集成到对话循环
3. 配置项（`config.yaml`）

### Phase 3: 记忆压缩（1-2 天）
1. 设计记忆压缩策略
2. 实现记忆压缩逻辑
3. 集成到记忆系统

### Phase 4: 优化和测试（1 天）
1. 性能测试
2. 边界测试
3. 文档更新

**总工作量估计**：4-6 天

---

## 待明天讨论的问题

1. **压缩后内容处理方式**：A/B/C/D？
2. **Token 估算方式**：tiktoken vs 简单估算？
3. **摘要模型选择**：主模型 vs 便宜模型 vs 可配置？
4. **存储策略**：内存 vs 文件 vs 记忆系统？
5. **记忆压缩触发时机**：数量阈值？时间阈值？
6. **与现有记忆系统的集成方式**？

---

## 参考资源

- **Hermes-agent 实现**: `hermes-agent/agent/context_compressor.py`
- **Hermes-agent 引擎接口**: `hermes-agent/agent/context_engine.py`
- **Hermes-agent 记忆管理**: `hermes-agent/agent/memory_manager.py`
- **LightHermes 当前记忆系统**: `lighthermes/memory.py`
- **LightHermes 核心引擎**: `lighthermes/core.py`

---

**下次会话**：继续讨论待定问题，细化设计方案
